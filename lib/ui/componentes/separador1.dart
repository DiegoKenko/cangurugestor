import 'package:flutter/material.dart';

class Separador1 extends StatelessWidget {
  const Separador1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      height: 0.5,
      color: Colors.black,
    );
  }
}
